# GitHub Pages Ready

## Recommended Repo Name

`echo-drift`

## Your Public URL

Once pushed and Pages is enabled, your public sample URL should be:

`https://dickson202.github.io/echo-drift/`

## What Is Already Ready

- `index.html` is the public landing page
- `styles.css` and `app.js` support the browser sample
- `.github/workflows/pages.yml` deploys the site automatically on pushes to `main`
- `.nojekyll` prevents GitHub Pages processing issues

## What To Upload

Push this whole folder to a GitHub repository named `echo-drift`.

## What To Paste Into Handshake

Paste:

`https://dickson202.github.io/echo-drift/`
